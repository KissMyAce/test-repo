# API Contract Routes

These routes implement the Step 5 API contract from `docs/auth-rbac-profile-plan.md`.

Base URL: `/api/v1`

- `/auth`
- `/profile`
- `/admin`
- `/uploads`

All handlers are scaffolded and intentionally return `501 NOT_IMPLEMENTED`
until service logic is implemented in the next steps.
